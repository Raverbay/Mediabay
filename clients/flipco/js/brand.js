(async()=>{const ps=await FLIPCO.load(),usable=p=>FLIPCO.stock(p)>0||p.available===true,e=FLIPCO.esc,brands=[...new Set(ps.filter(usable).map(p=>p.brand))].sort();document.querySelector('#brands').innerHTML=brands.length?brands.map((b,i)=>`<a href="shop.html?brand=${encodeURIComponent(b)}"><span>${String(i+1).padStart(2,'0')}</span><h2>${e(b)}</h2><small>${ps.filter(p=>usable(p)&&p.brand===b).length} PIECES</small><i>↗</i></a>`).join(''):'<p class="muted">Brand index in aggiornamento.</p>'})();
